<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/item_verify_doc"
        android:title="Verify testing document"
        app:showAsAction="never" />
    <item android:id="@+id/item_all_attestations"
        android:title="Get all attestations"
        app:showAsAction="never" />
    <item android:id="@+id/item_fact_response"
        android:title="Respond to attestation request"
        app:showAsAction="never" />

    <item
        android:id="@+id/item_request_fact"
        android:title="Request attestation"
        app:showAsAction="never">
        <menu>

        </menu>
    </item>
</menu>